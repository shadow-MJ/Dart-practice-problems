void main(){
  var student = ["joy","tonmoy","tanvir","nahid","nafi"];

  print(student);
  print("Length: ${student.length}");
  print(student.single);  //only used when there is only one element in the list
}